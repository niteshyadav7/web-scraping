╔══════════════════════════════════════════════════════════════╗
║         RENDER.COM DEPLOYMENT - QUICK REFERENCE              ║
╚══════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────┐
│ 📋 DEPLOYMENT CHECKLIST                                      │
└──────────────────────────────────────────────────────────────┘

  ✅ Files Created:
     □ Procfile
     □ requirements.txt (updated)
     □ render-build.sh
     □ runtime.txt
     □ .gitignore
     □ api.py (updated for production)

  ✅ GitHub Setup:
     □ Create GitHub repository
     □ Push code to GitHub
     □ Verify all files are there

  ✅ Render Setup:
     □ Create Render.com account
     □ Connect GitHub repository
     □ Configure web service
     □ Deploy!

┌──────────────────────────────────────────────────────────────┐
│ 🚀 QUICK DEPLOYMENT STEPS                                    │
└──────────────────────────────────────────────────────────────┘

  1. PUSH TO GITHUB:
     git init
     git add .
     git commit -m "Deploy to Render"
     git remote add origin https://github.com/YOUR_USERNAME/flipkart-scraper.git
     git push -u origin main

  2. CREATE RENDER SERVICE:
     → Go to render.com
     → Click "New +" → "Web Service"
     → Connect GitHub repo
     → Configure settings (see below)
     → Click "Create Web Service"

  3. WAIT FOR DEPLOYMENT:
     → Watch build logs (5-10 min)
     → Get your URL
     → Test: https://YOUR-APP.onrender.com/api/status

┌──────────────────────────────────────────────────────────────┐
│ ⚙️ RENDER CONFIGURATION                                      │
└──────────────────────────────────────────────────────────────┘

  Name:           flipkart-scraper-api
  Runtime:        Python 3
  Build Command:  bash render-build.sh
  Start Command:  gunicorn api:app
  Instance Type:  Free

  Environment Variables:
    PYTHON_VERSION = 3.11.0

┌──────────────────────────────────────────────────────────────┐
│ 🔗 UPDATE FRONTEND                                           │
└──────────────────────────────────────────────────────────────┘

  Edit: frontend/src/App.jsx

  Change:
    const API_URL = 'http://localhost:5000'

  To:
    const API_URL = 'https://YOUR-APP.onrender.com'

  Then rebuild frontend:
    cd frontend
    npm run build

┌──────────────────────────────────────────────────────────────┐
│ ⚠️ FREE TIER LIMITATIONS                                     │
└──────────────────────────────────────────────────────────────┘

  • Spins down after 15 min inactivity
  • Cold start: 15-30 seconds
  • 512 MB RAM
  • 750 hours/month

  Tips:
    - Reduce max_pages to 20-50
    - Scrape fewer products at once
    - Ping API every 10 min to keep alive

┌──────────────────────────────────────────────────────────────┐
│ 🧪 TESTING DEPLOYMENT                                        │
└──────────────────────────────────────────────────────────────┘

  1. Test API Status:
     https://YOUR-APP.onrender.com/api/status

     Should return:
     {"is_running": false, "status": "idle", ...}

  2. Test Scraping:
     Use frontend to start a scrape
     Check Render logs for progress

  3. Monitor Logs:
     Render Dashboard → Your Service → Logs

┌──────────────────────────────────────────────────────────────┐
│ 🐛 TROUBLESHOOTING                                           │
└──────────────────────────────────────────────────────────────┘

  Build Fails:
    → Check build logs in Render
    → Ensure all files pushed to GitHub
    → Verify render-build.sh syntax

  Chrome Not Found:
    → Check if "Installing Chrome..." in logs
    → Build script may have failed
    → Try redeploying

  Timeout Errors:
    → Reduce max_pages (try 20)
    → Scrape 1 product at a time
    → Free tier has limited resources

  Slow Response:
    → Normal for free tier
    → Cold start takes 15-30 sec
    → Consider paid tier for always-on

┌──────────────────────────────────────────────────────────────┐
│ 💰 COST OPTIONS                                              │
└──────────────────────────────────────────────────────────────┘

  Free:     $0/month  - 750 hrs, sleeps, 512MB
  Starter:  $7/month  - Always on, 512MB
  Standard: $25/month - Always on, 2GB RAM

  Recommendation: Start free, upgrade if needed

┌──────────────────────────────────────────────────────────────┐
│ 📊 MONITORING                                                │
└──────────────────────────────────────────────────────────────┘

  Render Dashboard:
    • View deployment status
    • Check build logs
    • Monitor resource usage
    • See request metrics

  Logs:
    • Real-time scraping progress
    • Error messages
    • Chrome/Selenium output

┌──────────────────────────────────────────────────────────────┐
│ 🎯 NEXT STEPS AFTER DEPLOYMENT                               │
└──────────────────────────────────────────────────────────────┘

  1. Update frontend API URL
  2. Test scraping with deployed backend
  3. Deploy frontend (optional)
  4. Share URL with others
  5. Monitor usage and logs
  6. Upgrade if needed

╔══════════════════════════════════════════════════════════════╗
║  Ready to deploy! Follow RENDER_DEPLOYMENT.md 🚀            ║
╚══════════════════════════════════════════════════════════════╝
